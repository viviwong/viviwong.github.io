# 主页网站完整技术方案

---

## 一、整体概览

这是一个**纯静态文档站点**，使用 Docusaurus 框架构建，通过 GitHub Actions 自动部署到 GitHub Pages。

```
本地写 Markdown/MDX
    ↓
git push → main 分支
    ↓
GitHub Actions 自动触发（.github/workflows/deploy.yml）
    ↓
ubuntu 服务器执行 npm install + npm run build
    ↓
生成 /build 静态文件
    ↓
部署到 GitHub Pages → https://viviwong.github.io
```

---

## 二、技术栈总览

### 核心框架

| 层次 | 技术 | 版本 |
|---|---|---|
| 文档框架 | **Docusaurus** | v3.9.2 |
| UI 库 | **React** | v19 |
| 配置语言 | **TypeScript** | ~5.6 |
| 内容格式 | **MDX / Markdown** | MDX v3 |

### 附加插件

| 功能 | 工具 |
|---|---|
| 在线 CMS 编辑 | **TinaCMS** v3.7 |
| 代码高亮 | Prism React Renderer |
| 图标库 | Remixicon CDN |
| 字体 | Google Fonts · Inter |
| 访问统计 | **不蒜子** busuanzi |
| 广告变现 | Google AdSense |

---

## 三、目录结构

```
001-gitpags/
├── docs/          → AI 学科文档（/docs/）
├── embedded/      → 嵌入式学科文档（/embedded/）
├── training/      → 实训笔记（/training/）
├── blog/          → 博客
├── static/        → 静态资源（图片、工具页 HTML、讲义）
│   └── web/       → 独立 HTML 工具页（打分系统、串口工具等）
└── src/           → React 自定义组件 / 主题 CSS
```

---

## 四、部署方式

**GitHub Actions 自动化流程**（`.github/workflows/deploy.yml`）

```yaml
on:
  push:
    branches: [ main ]   # 推送到 main 分支自动触发

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - npm install
      - npm run build     # 构建静态文件到 /build

  deploy:
    - actions/deploy-pages@v4   # 部署到 GitHub Pages
```

- 托管平台：**GitHub Pages**（免费 CDN）
- 线上地址：`https://viviwong.github.io`
- 无后端服务器，无数据库（主站）

---

## 五、主站 vs 独立工具页的对比

| | 主站（Docusaurus） | 独立 HTML 工具页 |
|---|---|---|
| 内容存储 | `.md` / `.mdx` 文件 | 无内容存储 |
| 数据库 | ❌ 无 | 部分有（见下节） |
| 后端 | ❌ 无 | ❌ 无 |
| 更新方式 | 改文件 + push | 改 HTML + push |
| 示例 | 课程讲义、实训文档 | 打分系统、串口工具 |

---

## 六、106-竞技打分系统 数据库方案

`static/web/106-竞技打分系统.html` 是唯一使用数据库的页面，采用**双层存储架构**。

### 架构图

```
浏览器端 (localStorage)  ←→  云端 (Supabase · PostgreSQL)
        ↑                           ↑
    本地读写                    实时同步/多端共享
```

### 1. 本地层 — localStorage

```js
const DB = {
  getPrograms: () => JSON.parse(localStorage.getItem('sc_programs') || '[]'),
  savePrograms: d  => { localStorage.setItem('sc_programs', ...); CloudSync.push() },
  // 同理：getJudges / saveJudges / getScores / saveScores / getSettings / saveSettings
}
```

所有数据（节目、裁判、打分、设置）先读写 `localStorage`，每次写完自动触发云同步。

### 2. 云端层 — Supabase（PostgreSQL）

| 项目 | 值 |
|---|---|
| 服务 | **Supabase**（基于 PostgreSQL 的 BaaS） |
| 表名 | `app_data` |
| 行 ID | `scoring_106` |
| 存储方式 | 整个状态存成一条 JSON 记录（upsert） |

```js
// 写入云端（upsert = insert or update）
await this.sb.from('app_data').upsert({
  id: 'scoring_106',
  data: { programs, judges, scores, session },
  updated_at: new Date().toISOString()
})
```

### 3. 实时同步 — Supabase Realtime

```js
// 监听 PostgreSQL 表的 UPDATE 事件
this.channel = this.sb.channel('realtime:106')
  .on('postgres_changes', {
    event: 'UPDATE', schema: 'public', table: 'app_data'
  }, payload => {
    // 收到推送 → 更新 localStorage → 刷新页面
    renderBoard()
  })
  .subscribe()
```

**效果**：管理员在一台电脑操作 → 所有裁判的手机/电脑屏幕**实时刷新**，无需手动刷新页面。

### 4. 降级策略

```js
try {
    this.sb = supabase.createClient(URL, KEY)
    await this.pull()
} catch(e) {
    // Supabase 不可用 → 自动降级为纯本地模式
    // 仍然可以本地打分，只是不能多端同步
}
```

---

## 七、为什么主站不需要数据库

课程讲义、实训文档这类内容**只读不写**，不需要用户登录、不需要实时数据，静态站完全满足需求：

- 🆓 托管免费（GitHub Pages）
- ⚡ 加载极快（纯 HTML，无服务器请求）
- 🔒 没有数据库就没有 SQL 注入风险
- 📝 内容用 Git 管理，天然有版本历史记录
